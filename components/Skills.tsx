import SectionTitle from "./SectionTitle";
import LightContainer from "./containers/Light";

const Skills = () => {
  return (
    <LightContainer>
      <section className="py-[72px] px-20">
        <SectionTitle title="My skills" />
      </section>
    </LightContainer>
  );
};

export default Skills;
